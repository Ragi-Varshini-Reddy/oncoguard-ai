# Phase 2 Tests Placeholder

Add tests under:

```text
tests/
```

Suggested tests:

- missing histopathology returns `status = missing`
- invalid file returns `status = error`
- artifact inference returns 256-dimensional embedding
- explanation payload exists when modality is available
- module satisfies `ModuleOutput`
