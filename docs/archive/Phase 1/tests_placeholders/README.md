# Phase 1 Tests Placeholder

Add tests under:

```text
tests/
```

Suggested tests:

- intraoral missing modality returns `status = missing`
- intraoral artifact inference returns 256-dimensional embedding
- clinical missing fields are imputed safely
- clinical artifact inference returns 128-dimensional embedding
- both modules satisfy `ModuleOutput`
