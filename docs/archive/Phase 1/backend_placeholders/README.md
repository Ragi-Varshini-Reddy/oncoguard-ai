# Phase 1 Backend Placeholder

Add Phase 1 API routes here conceptually, then implement them inside the real FastAPI app under:

```text
backend/app/
```

Recommended future files:

```text
backend/app/phase1_routes.py
backend/app/phase1_schemas.py
```

Keep all responses compatible with `src/common/contracts.py`.
